[build]
  command = "pnpm --filter room-service build"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- apps/room-service pnpm-lock.yaml pnpm-workspace.yaml"
  publish = "apps/room-service/dist"