[build]
  command = "pnpm --filter smoking-room build"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- apps/smoking-room pnpm-lock.yaml pnpm-workspace.yaml"
  publish = "apps/smoking-room/dist"