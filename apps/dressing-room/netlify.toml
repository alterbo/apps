[build]
  command = "pnpm --filter dressing-room build"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- apps/dressing-room pnpm-lock.yaml pnpm-workspace.yaml"
  publish = "apps/dressing-room/dist"